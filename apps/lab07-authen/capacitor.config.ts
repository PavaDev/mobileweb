import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cpkku.pavarit.lab07',
  appName: 'cpkku.pavarit.lab07',
  webDir: 'dist'
};

export default config;
